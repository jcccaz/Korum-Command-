# KorumOS Intelligence Lifecycle: Decision Integrity Workflow

This document provides the high-fidelity A-to-D map of a **Validated Intelligence Estimate (VIE)**. Unlike standard AI tools, KorumOS is an **Adversarial Governance System** where standard providers are used as modular execution engines.

---

## Phase A: Security & Minimization (The Falcon Layer)

Every mission begins with the **Falcon Protocol** — our "Zero-Exposure" firewall.

1.  **Workflow DNA Injection**: Based on the query or user selection, a specialized **Workflow DNA** (e.g., *Genealogy, Cyber, or Finance*) is loaded. This dictates the council's "mood," risk bias, and output structure.
2.  **Falcon Protocol Scrubbing**: The raw query passes through the **Falcon Layer**. All PII/PHI (Names, Emails, SSNs, IPs) is replaced with deterministic pseudonyms (e.g., `[PERSON_01]`). 
    *   *Result*: The AI operates on semantic "signals" without ever seeing the raw sensitive data.
3.  **Prompt Enhancement**: The de-identified query is "sharpened" into an intelligence-grade prompt, adding specific domain requirements and forensic directives.

## Phase B: Deliberation (The Execution Layer)

KorumOS convenes a **Sequential Council** of five independent AI providers (OpenAI, Anthropic, Google, Perplexity, Mistral).

1.  **Step-by-Step Expansion**: 
    *   **Card 1 (Analyst)** produces the baseline.
    *   **Card 2 (Architect)** reads Card 1's work and builds the structure.
    *   **Card 3 (Critic/Red Team)** reads both and identifies flaws, blind spots, or cognitive biases.
    *   **Card 4/5** integrate and refine the final intelligence pool.
2.  **Adversarial Hardening**: If **Red Team Mode** is active, a dedicated pass "attacks" the internal logic of the council, forcing a higher standard of proof.

## Phase C: Intelligence Synthesis (The Governor Layer)

The raw council results are then processed by the **VIE Engine**, overseen by the **Confidence Governor**.

1.  **Truth Scoring**: Each major claim is compared across all five providers. If they disagree, the claim is marked as `CONFLICTING`. If they agree, it's `VERIFIED`.
2.  **Confidence Governor**: The system runs a final "Diagnostic Pass."
    *   It checks for **Source Concentration Risk** (e.g., did 80% of information come from one AI?).
    *   It applies **Scoring Floors** (e.g., 62% for 3+ verified facts) and **Hard Caps** (e.g., 45% for unverified root causes).
3.  **Schema Enforcement**: The output is mapped strictly to the **DECISION_PACKET.MD** schema. If it doesn't fit, the system forces a re-composition.

## Phase D: Provenance & Audit (The Ledger Layer)

Every mission ends by generating an immutable record in the **Audit Trail Ledger (ATL)**.

1.  **Validated Intelligence Estimate (VIE)**: The final brief is generated with **Scans & Signals**, removing AI "chatter" and applying high-contrast themes (e.g., *Carbon Steel*).
2.  **Tamper-Evident Chain**: Every event in the mission lifecycle — from the Falcon hash to the final VIE — is hashed and chained in a cryptographic ledger.
3.  **Decision Provenance**: This provides a "Black Box" trace that allow auditors to prove *why* a decision was made, with an auditable path back to the cross-verified council deliberation.

---
*Produced by Korum-OS Decision Intelligence / Operational Flow v2.0*
